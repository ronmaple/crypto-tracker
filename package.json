{
	"name": "crypto-remake",
	"version": "1.0.0",
	"main": "server/index.js",
	"author": "<ronmapue@gmail.com>",
	"license": "MIT",
	"scripts": {
		"start": "cd server && yarn run start",
		"heroku-postbuild": "cd client && yarn install && yarn run build && cd .. && cd server && yarn install"
	}
}
